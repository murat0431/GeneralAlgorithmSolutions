package test;

import impl.PermSequence;

public class PermSequenceTest {
    public static void main(String[] args) {
        PermSequence ps = new PermSequence();
        System.out.println(ps.getPermutation(3, 2));
    }
}
